// splice removes any number of consecutive elements from an index.

let arr_ = ['today', 'was', 'not', 'so', 'great'];
let splicedarr = arr_.splice(2, 2);
console.log(splicedarr);

const arr = [2, 4, 5, 1, 7, 5, 2, 1];
// Only change code below this line

arr.splice(0, 1);
arr.splice(3, 4);

// Only change code above this line
console.log(arr);

